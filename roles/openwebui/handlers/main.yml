---
- name: Restart Open WebUI docker compose deployment
  ansible.builtin.command:
    cmd: docker compose --ansi never --progress plain restart
    chdir: "{{ openwebui_docker_path }}"
  changed_when: true
  listen: restart openwebui
